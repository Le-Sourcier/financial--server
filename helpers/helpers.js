const { serverMessage } = require("./components/serverMessage");
const { generateToken, generateUID } = require("./components/functions");

module.exports = { serverMessage, generateToken, generateUID };
